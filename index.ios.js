var React = require('react-native');
var Dismensions = require('Dismensions');
var {StyleSheet,Text,AppRegister,View,Image,ScrollView,TabBarIOS} = React;

var width = Dismensions.get('window').width;
var height = Dismensions.get('window').height - 70;

var demo_action = React.createClass({
    getInitialState:function () {
        return {
            tab:'message'
        }
    },
    select:function (tabName) {
        this.
    }
});

var styles = StyleSheet.create({
    flex:{
        flex:1
    }
});